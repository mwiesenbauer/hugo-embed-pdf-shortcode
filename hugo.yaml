module:
  hugoVersion:
    extended: false
    min: "0.80.0"

  mounts:
    - source: assets
      target: assets/embed-pdf

    - source: layouts
      target: layouts

params:
  pdfjs:
    Src: "https://esm.run/pdfjs-dist"
    WorkerSrc: "https://esm.run/pdfjs-dist/build/pdf.worker.mjs"
